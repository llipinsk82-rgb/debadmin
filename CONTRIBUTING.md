# Contributing

This project is developed iteratively.

## Rules

- Work on `develop`.
- Keep changes small and logical.
- Keep Bash readable and dependency-light.
- Put reusable logic in `lib/`.
- Put user-facing commands in `modules/`.
- Test on Debian 12 where possible.
